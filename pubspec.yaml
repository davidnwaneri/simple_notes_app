name: simple_notes_app
description: Simple notes app with Appwrite

version: 0.1.0

environment:
  sdk: ">=2.19.6 <3.0.0"

dependencies:
  flex_color_scheme: ^6.1.2
  flutter:
    sdk: flutter
  go_router: ^6.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^4.0.0+1

flutter:
  uses-material-design: true